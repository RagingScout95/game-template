package com.pixelgame.engine.model.enums;

public enum MovementMode {
    FREE,              // Fully free movement
    HORIZONTAL_ONLY,   // Can only move left/right
    VERTICAL_ONLY,     // Can only move up/down
    LOCKED             // No movement allowed
}

